// Shell command execution — JNI up-call to Java bridge.
// Used for: am start, pm commands, dumpsys, settings, input keyevent, etc.
// Implementation routes through bridge.cpp: exec_shell() and launch_app()
