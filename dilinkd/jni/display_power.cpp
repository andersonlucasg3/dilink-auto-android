// Display power management — JNI up-call to Java bridge.
// Uses DisplayControl (loaded from services.jar) or cmd display power-off as fallback.
// Implementation routes through bridge.cpp: set_display_power()
